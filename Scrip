local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local player = Players.LocalPlayer

local isDupeOn = false
local incrementValue = 1

-- ==================== FUNÇÃO DE FORMATAÇÃO ====================
local function formatNumber(n)
    if n >= 1000000 then
        return string.format("%.1fM", n / 1000000):gsub("%.0", "")
    elseif n >= 1000 then
        return string.format("%.0fk", n / 1000)
    else
        return tostring(n)
    end
end

-- ==================== UI SETUP ====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ttkzwzzHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = player:FindFirstChild("PlayerGui") or game:GetService("CoreGui")

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 200, 0, 100)
MainFrame.Position = UDim2.new(0.5, -100, 0.5, -50)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 5, 5)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 2
MainStroke.Color = Color3.fromRGB(255, 0, 0)
MainStroke.Parent = MainFrame

-- Efeito Pulsante
task.spawn(function()
    while true do
        TweenService:Create(MainStroke, TweenInfo.new(1), {Transparency = 0.5}):Play()
        task.wait(1)
        TweenService:Create(MainStroke, TweenInfo.new(1), {Transparency = 0}):Play()
        task.wait(1)
    end
end)

-- ==================== ELEMENTOS ====================
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.Text = "ttkzwzz Dupe"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 12

local ValueInput = Instance.new("TextBox", MainFrame)
ValueInput.Size = UDim2.new(0, 50, 0, 20)
ValueInput.Position = UDim2.new(0, 10, 0, 40)
ValueInput.BackgroundColor3 = Color3.fromRGB(30, 5, 5)
ValueInput.TextColor3 = Color3.fromRGB(255, 255, 255)
ValueInput.Text = "1"
ValueInput.PlaceholderText = "Inc."
ValueInput.Parent = MainFrame

local ToggleSwitch = Instance.new("TextButton", MainFrame)
ToggleSwitch.Size = UDim2.new(0, 100, 0, 20)
ToggleSwitch.Position = UDim2.new(0, 70, 0, 40)
ToggleSwitch.BackgroundColor3 = Color3.fromRGB(40, 5, 5)
ToggleSwitch.Text = "OFF"
ToggleSwitch.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleSwitch.Font = Enum.Font.GothamBold

ToggleSwitch.MouseButton1Click:Connect(function()
    isDupeOn = not isDupeOn
    ToggleSwitch.Text = isDupeOn and "ON" or "OFF"
    ToggleSwitch.BackgroundColor3 = isDupeOn and Color3.fromRGB(200, 0, 0) or Color3.fromRGB(40, 5, 5)
end)

ValueInput.FocusLost:Connect(function() incrementValue = tonumber(ValueInput.Text) or 1 end)

-- ==================== DRAGGING SYSTEM ====================
local uis = game:GetService("UserInputService")
local dragging, dragInput, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true; dragStart = input.Position; startPos = MainFrame.Position
    end
end)
uis.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
uis.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)

-- ==================== LÓGICA DE DUPLICAÇÃO ====================
task.spawn(function()
    while true do
        task.wait(0.5)
        if isDupeOn then
            local coreGui = player:FindFirstChild("PlayerGui")
            if coreGui then
                for _, node in pairs(coreGui:GetDescendants()) do
                    if node:IsA("TextLabel") and string.find(node.Text, "x") then
                        -- Extrai o número atual (suporta formatos como 1k, 1M ou apenas números)
                        local text = node.Text:gsub("x", "")
                        local currentVal = 0
                        
                        if text:find("k") then currentVal = tonumber(text:gsub("k", "")) * 1000
                        elseif text:find("M") then currentVal = tonumber(text:gsub("M", "")) * 1000000
                        else currentVal = tonumber(text) or 0 end
                        
                        -- Atualiza com formatação
                        node.Text = "x" .. formatNumber(currentVal + incrementValue)
                    end
                end
            end
        end
    end
end)
